// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"ikik-api/ent/predicate"
	"ikik-api/ent/shopbalanceledger"
	"ikik-api/ent/shopdrawcycle"
	"ikik-api/ent/shoporder"
	"ikik-api/ent/shopproduct"
	"ikik-api/ent/user"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ShopDrawCycleQuery is the builder for querying ShopDrawCycle entities.
type ShopDrawCycleQuery struct {
	config
	ctx               *QueryContext
	order             []shopdrawcycle.OrderOption
	inters            []Interceptor
	predicates        []predicate.ShopDrawCycle
	withUser          *UserQuery
	withProduct       *ShopProductQuery
	withOrders        *ShopOrderQuery
	withBalanceLedger *ShopBalanceLedgerQuery
	modifiers         []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ShopDrawCycleQuery builder.
func (_q *ShopDrawCycleQuery) Where(ps ...predicate.ShopDrawCycle) *ShopDrawCycleQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ShopDrawCycleQuery) Limit(limit int) *ShopDrawCycleQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ShopDrawCycleQuery) Offset(offset int) *ShopDrawCycleQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ShopDrawCycleQuery) Unique(unique bool) *ShopDrawCycleQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ShopDrawCycleQuery) Order(o ...shopdrawcycle.OrderOption) *ShopDrawCycleQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryUser chains the current query on the "user" edge.
func (_q *ShopDrawCycleQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shopdrawcycle.Table, shopdrawcycle.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shopdrawcycle.UserTable, shopdrawcycle.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProduct chains the current query on the "product" edge.
func (_q *ShopDrawCycleQuery) QueryProduct() *ShopProductQuery {
	query := (&ShopProductClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shopdrawcycle.Table, shopdrawcycle.FieldID, selector),
			sqlgraph.To(shopproduct.Table, shopproduct.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, shopdrawcycle.ProductTable, shopdrawcycle.ProductColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrders chains the current query on the "orders" edge.
func (_q *ShopDrawCycleQuery) QueryOrders() *ShopOrderQuery {
	query := (&ShopOrderClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shopdrawcycle.Table, shopdrawcycle.FieldID, selector),
			sqlgraph.To(shoporder.Table, shoporder.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, shopdrawcycle.OrdersTable, shopdrawcycle.OrdersColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryBalanceLedger chains the current query on the "balance_ledger" edge.
func (_q *ShopDrawCycleQuery) QueryBalanceLedger() *ShopBalanceLedgerQuery {
	query := (&ShopBalanceLedgerClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(shopdrawcycle.Table, shopdrawcycle.FieldID, selector),
			sqlgraph.To(shopbalanceledger.Table, shopbalanceledger.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, shopdrawcycle.BalanceLedgerTable, shopdrawcycle.BalanceLedgerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ShopDrawCycle entity from the query.
// Returns a *NotFoundError when no ShopDrawCycle was found.
func (_q *ShopDrawCycleQuery) First(ctx context.Context) (*ShopDrawCycle, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{shopdrawcycle.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ShopDrawCycleQuery) FirstX(ctx context.Context) *ShopDrawCycle {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ShopDrawCycle ID from the query.
// Returns a *NotFoundError when no ShopDrawCycle ID was found.
func (_q *ShopDrawCycleQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{shopdrawcycle.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ShopDrawCycleQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ShopDrawCycle entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ShopDrawCycle entity is found.
// Returns a *NotFoundError when no ShopDrawCycle entities are found.
func (_q *ShopDrawCycleQuery) Only(ctx context.Context) (*ShopDrawCycle, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{shopdrawcycle.Label}
	default:
		return nil, &NotSingularError{shopdrawcycle.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ShopDrawCycleQuery) OnlyX(ctx context.Context) *ShopDrawCycle {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ShopDrawCycle ID in the query.
// Returns a *NotSingularError when more than one ShopDrawCycle ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ShopDrawCycleQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{shopdrawcycle.Label}
	default:
		err = &NotSingularError{shopdrawcycle.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ShopDrawCycleQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ShopDrawCycles.
func (_q *ShopDrawCycleQuery) All(ctx context.Context) ([]*ShopDrawCycle, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ShopDrawCycle, *ShopDrawCycleQuery]()
	return withInterceptors[[]*ShopDrawCycle](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ShopDrawCycleQuery) AllX(ctx context.Context) []*ShopDrawCycle {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ShopDrawCycle IDs.
func (_q *ShopDrawCycleQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(shopdrawcycle.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ShopDrawCycleQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ShopDrawCycleQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ShopDrawCycleQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ShopDrawCycleQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ShopDrawCycleQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ShopDrawCycleQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ShopDrawCycleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ShopDrawCycleQuery) Clone() *ShopDrawCycleQuery {
	if _q == nil {
		return nil
	}
	return &ShopDrawCycleQuery{
		config:            _q.config,
		ctx:               _q.ctx.Clone(),
		order:             append([]shopdrawcycle.OrderOption{}, _q.order...),
		inters:            append([]Interceptor{}, _q.inters...),
		predicates:        append([]predicate.ShopDrawCycle{}, _q.predicates...),
		withUser:          _q.withUser.Clone(),
		withProduct:       _q.withProduct.Clone(),
		withOrders:        _q.withOrders.Clone(),
		withBalanceLedger: _q.withBalanceLedger.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ShopDrawCycleQuery) WithUser(opts ...func(*UserQuery)) *ShopDrawCycleQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUser = query
	return _q
}

// WithProduct tells the query-builder to eager-load the nodes that are connected to
// the "product" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ShopDrawCycleQuery) WithProduct(opts ...func(*ShopProductQuery)) *ShopDrawCycleQuery {
	query := (&ShopProductClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withProduct = query
	return _q
}

// WithOrders tells the query-builder to eager-load the nodes that are connected to
// the "orders" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ShopDrawCycleQuery) WithOrders(opts ...func(*ShopOrderQuery)) *ShopDrawCycleQuery {
	query := (&ShopOrderClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrders = query
	return _q
}

// WithBalanceLedger tells the query-builder to eager-load the nodes that are connected to
// the "balance_ledger" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ShopDrawCycleQuery) WithBalanceLedger(opts ...func(*ShopBalanceLedgerQuery)) *ShopDrawCycleQuery {
	query := (&ShopBalanceLedgerClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withBalanceLedger = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ShopDrawCycle.Query().
//		GroupBy(shopdrawcycle.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ShopDrawCycleQuery) GroupBy(field string, fields ...string) *ShopDrawCycleGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ShopDrawCycleGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = shopdrawcycle.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ShopDrawCycle.Query().
//		Select(shopdrawcycle.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *ShopDrawCycleQuery) Select(fields ...string) *ShopDrawCycleSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ShopDrawCycleSelect{ShopDrawCycleQuery: _q}
	sbuild.label = shopdrawcycle.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ShopDrawCycleSelect configured with the given aggregations.
func (_q *ShopDrawCycleQuery) Aggregate(fns ...AggregateFunc) *ShopDrawCycleSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ShopDrawCycleQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !shopdrawcycle.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ShopDrawCycleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ShopDrawCycle, error) {
	var (
		nodes       = []*ShopDrawCycle{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withUser != nil,
			_q.withProduct != nil,
			_q.withOrders != nil,
			_q.withBalanceLedger != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ShopDrawCycle).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ShopDrawCycle{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withUser; query != nil {
		if err := _q.loadUser(ctx, query, nodes, nil,
			func(n *ShopDrawCycle, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withProduct; query != nil {
		if err := _q.loadProduct(ctx, query, nodes, nil,
			func(n *ShopDrawCycle, e *ShopProduct) { n.Edges.Product = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOrders; query != nil {
		if err := _q.loadOrders(ctx, query, nodes,
			func(n *ShopDrawCycle) { n.Edges.Orders = []*ShopOrder{} },
			func(n *ShopDrawCycle, e *ShopOrder) { n.Edges.Orders = append(n.Edges.Orders, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withBalanceLedger; query != nil {
		if err := _q.loadBalanceLedger(ctx, query, nodes,
			func(n *ShopDrawCycle) { n.Edges.BalanceLedger = []*ShopBalanceLedger{} },
			func(n *ShopDrawCycle, e *ShopBalanceLedger) { n.Edges.BalanceLedger = append(n.Edges.BalanceLedger, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ShopDrawCycleQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*ShopDrawCycle, init func(*ShopDrawCycle), assign func(*ShopDrawCycle, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ShopDrawCycle)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ShopDrawCycleQuery) loadProduct(ctx context.Context, query *ShopProductQuery, nodes []*ShopDrawCycle, init func(*ShopDrawCycle), assign func(*ShopDrawCycle, *ShopProduct)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*ShopDrawCycle)
	for i := range nodes {
		fk := nodes[i].ProductID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(shopproduct.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "product_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ShopDrawCycleQuery) loadOrders(ctx context.Context, query *ShopOrderQuery, nodes []*ShopDrawCycle, init func(*ShopDrawCycle), assign func(*ShopDrawCycle, *ShopOrder)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*ShopDrawCycle)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(shoporder.FieldDrawCycleID)
	}
	query.Where(predicate.ShopOrder(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(shopdrawcycle.OrdersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.DrawCycleID
		if fk == nil {
			return fmt.Errorf(`foreign-key "draw_cycle_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "draw_cycle_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *ShopDrawCycleQuery) loadBalanceLedger(ctx context.Context, query *ShopBalanceLedgerQuery, nodes []*ShopDrawCycle, init func(*ShopDrawCycle), assign func(*ShopDrawCycle, *ShopBalanceLedger)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*ShopDrawCycle)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(shopbalanceledger.FieldDrawCycleID)
	}
	query.Where(predicate.ShopBalanceLedger(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(shopdrawcycle.BalanceLedgerColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.DrawCycleID
		if fk == nil {
			return fmt.Errorf(`foreign-key "draw_cycle_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "draw_cycle_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *ShopDrawCycleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ShopDrawCycleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(shopdrawcycle.Table, shopdrawcycle.Columns, sqlgraph.NewFieldSpec(shopdrawcycle.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, shopdrawcycle.FieldID)
		for i := range fields {
			if fields[i] != shopdrawcycle.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withUser != nil {
			_spec.Node.AddColumnOnce(shopdrawcycle.FieldUserID)
		}
		if _q.withProduct != nil {
			_spec.Node.AddColumnOnce(shopdrawcycle.FieldProductID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ShopDrawCycleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(shopdrawcycle.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = shopdrawcycle.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *ShopDrawCycleQuery) ForUpdate(opts ...sql.LockOption) *ShopDrawCycleQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *ShopDrawCycleQuery) ForShare(opts ...sql.LockOption) *ShopDrawCycleQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// ShopDrawCycleGroupBy is the group-by builder for ShopDrawCycle entities.
type ShopDrawCycleGroupBy struct {
	selector
	build *ShopDrawCycleQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ShopDrawCycleGroupBy) Aggregate(fns ...AggregateFunc) *ShopDrawCycleGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ShopDrawCycleGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShopDrawCycleQuery, *ShopDrawCycleGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ShopDrawCycleGroupBy) sqlScan(ctx context.Context, root *ShopDrawCycleQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ShopDrawCycleSelect is the builder for selecting fields of ShopDrawCycle entities.
type ShopDrawCycleSelect struct {
	*ShopDrawCycleQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ShopDrawCycleSelect) Aggregate(fns ...AggregateFunc) *ShopDrawCycleSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ShopDrawCycleSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ShopDrawCycleQuery, *ShopDrawCycleSelect](ctx, _s.ShopDrawCycleQuery, _s, _s.inters, v)
}

func (_s *ShopDrawCycleSelect) sqlScan(ctx context.Context, root *ShopDrawCycleQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
