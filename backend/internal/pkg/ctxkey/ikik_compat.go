package ctxkey

// AuthenticatedUserID remains as a compatibility name for ikik modules.
// It intentionally uses the official UserID key so middleware and services share one value.
const AuthenticatedUserID Key = UserID
